# Plain git storage — do NOT use LFS for chunks
# -delta = skip delta search, huge CPU/RAM saving on incompressible .safetensors (was OOM with 8 threads)
model_chunks/** -delta -filter -diff -merge -text
*.part-* -delta -filter -diff -merge -text
